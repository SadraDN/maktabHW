
 Enum contactType = ContactTypeEnum.Email;
Console.WriteLine(" '{0}'",contactType.ToString());


NumOfContactType(ContactTypeEnum.Email);
NumOfContactType(ContactTypeEnum.Sms);

static int NumOfContactType(ContactTypeEnum text)
{
    return (int)(text);
}


string str = "Red";

ContactTypeEnum color = (ContactTypeEnum)Enum.Parse(typeof(ContactTypeEnum), str);





public string switchcaseEnum((int)
{

}